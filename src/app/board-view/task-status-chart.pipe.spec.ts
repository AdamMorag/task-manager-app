import { TaskStatusChartPipe } from './task-status-chart.pipe';

describe('TaskStatusChartPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskStatusChartPipe();
    expect(pipe).toBeTruthy();
  });
});
